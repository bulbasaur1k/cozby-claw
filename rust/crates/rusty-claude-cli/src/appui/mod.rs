//! Полноэкранное интерактивное приложение `cozby-claw-cli` (ratatui) — основной
//! интерфейс агента: история со стримингом, ввод, футер, подтверждения, темы.
//! Запускается по умолчанию в интерактивном терминале; pipe/`prompt` остаются на
//! строчном режиме.

mod protocol;
mod ui;
mod worker;

pub use ui::run;
